# M0.39 — Operator Audit Filtering, Correlation & Incident Context

Status: repository-side implemented; CI verification pending.

## Delivered
- Server-side, bounded history filters for actor, action, outcome, merchant and feed.
- Exact incident-to-history correlation by merchant external key + source key.
- Up to three relevant recovery outcomes directly on feed incident cards.
- Tests for filtering, exact correlation, missing feed context and context limits.
- No new mutations, permissions, RPCs or schema changes.

## Security boundary
History filtering remains read-only and authenticated. Free audit metadata and raw error text stay out of the UI. Filter query parameters never become mutation target inputs.

## Next
M0.40 — Operational Timeline & Feed State Transition Context: combine feed health/orchestration state and human recovery outcomes into a compact chronological timeline without adding new write capabilities.
