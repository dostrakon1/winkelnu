# M0.48 — Preview Supabase Provisioning & Migration Runbook

Status: repository-side implemented; exact HEAD CI pending. Live execution remains blocked on a real preview Supabase project.

## Delivered

- executable preview provisioning/migration runbook;
- exact migration manifest for `0001`–`0015`;
- CI-enforced migration order/count verification;
- preflight → migrate → live verify → generate types → bootstrap → operator acceptance → persistence switch sequence;
- explicit acceptance gate;
- explicit rollback/stop strategy;
- production boundary kept separate from preview acceptance.

## Safety properties

- no live Supabase state is claimed without project access;
- `CATALOG_PERSISTENCE=memory` remains the default until preview acceptance passes;
- migrations may not be skipped/reordered or patched only in the dashboard;
- preview failure does not justify promotion to production;
- no secrets are included in execution records;
- failed preview activation prefers stop/recreate over hidden manual schema drift.

## External gate

A real preview Supabase project is now the material dependency for the next activation phase. Required external inputs remain the project ref/URL, publishable key, service-role key and creation of the first explicit operator Auth account. Secrets must be supplied only through secure environment configuration, never Git.

## Next

M0.49 — Preview Activation Evidence Pack & Handoff Checklist: prepare one concise evidence record for the actual preview activation so every required output (commit SHA, migration state, readiness result, generated types, import evidence and operator acceptance) can be captured consistently when project access becomes available.
