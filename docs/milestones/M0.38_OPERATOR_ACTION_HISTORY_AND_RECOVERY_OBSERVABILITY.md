# M0.38 — Operator Action History & Recovery Observability

Status: repository-side implemented; CI verification pending.

## Opgeleverd
- `OperatorActionHistoryService` met begrensde recente history-read.
- Supabase server-side history repository op `operator_audit_events`.
- Alleen terminale `succeeded`/`failed` outcomes in de operatorhistorie.
- Veilige projection zonder vrije metadata of ruwe foutteksten.
- `/intern/operations` laadt incidenten en action history parallel.
- Recente acties tonen actor, rol, actie, merchant/feed, outcome en timestamp.
- Tests voor history service en limietbegrenzing.
- Geen nieuwe write-acties, permissies, tabellen of RPCs.

## Acceptance criteria
- History blijft achter de menselijke operator session boundary.
- Browser krijgt geen service-role credentials.
- Geen gevoelige auditmetadata wordt weergegeven.
- Bestaande M0.36/M0.37 recoverywrites blijven ongewijzigd en geauditeerd/idempotent.
- CI moet databasecontract, lint, typecheck, tests en production build laten slagen.

## Volgende logische stap
M0.39 — Operator Audit Filtering, Correlation & Incident Context.
