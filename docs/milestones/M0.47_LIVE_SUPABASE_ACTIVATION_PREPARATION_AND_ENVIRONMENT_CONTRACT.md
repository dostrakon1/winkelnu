# M0.47 — Live Supabase Activation Preparation & Environment Contract

Status: repository-side implemented; live Supabase activation remains an external gate.

## Delivered
- `.env.example` aligned to the actual Supabase Auth publishable-key contract;
- explicit separation of public Auth values and privileged server secrets;
- `scripts/verify-activation-environment.mjs`;
- `npm run verify:activation-env`;
- project URL/project ref consistency checks;
- service-role/publishable key separation check;
- operator allowlist/role validation including at least one owner;
- fail-closed persistence mode validation;
- updated Supabase setup sequence through migrations `0001`–`0015`;
- explicit one-time external prerequisites and preview activation order;
- architecture documentation for the live activation handoff.

## Security boundary
The activation verifier never prints Supabase keys. `SUPABASE_SERVICE_ROLE_KEY` remains server-only and must never be stored in source control or a `NEXT_PUBLIC_` variable.

## Completion gate
M0.47 makes the repository ready for a real preview connection, but it does not make the database live. A real Supabase preview project, project credentials, applied migrations and project-derived types are still required before `CATALOG_PERSISTENCE=supabase` can be accepted.

## Next logical phase
M0.48 — Preview Supabase Provisioning & Migration Runbook: prepare the exact migration/application and acceptance procedure that will be executed once the real preview project exists.
